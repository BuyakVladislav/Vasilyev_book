#include <iostream>

using namespace std;

// Напишите программу, в которой создается массив и заполняется нечетными натуральными числами

int main() {
	int a, b, c;
	cin >> a >> b >> c;

	if (a % 2 == 0 || b % 2 == 0 || c % 2 == 0)
	{
		cout << "Error!!!" << endl;
	}
	else
	{
		int arr[] = { a, b, c };
		cout << arr[0] << " " << arr[1] << " " << arr[2] << endl;
	}
	
}
