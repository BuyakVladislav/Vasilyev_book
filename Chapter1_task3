//Напишите программу для отображения в консоли чисел , которые при делении на 4 в остатке дают 3 (такие числа описываются по формуле 4k + 3, 
//где k принимает значения 0, 1, 2 и так далее). Количество чисел вводится пользователем с клавиатуры. 

#include <iostream>
#include <cstdlib>

using namespace std;

int main() {
	int n, k = 0, step = 0;
	cin >> n;
	while (step <= n) {
		cout << (4 * k) + 3 << endl;
		step++;
		k++;
	}

	return 0;
}
